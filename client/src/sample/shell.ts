/**
 * The app shell's sample tenant: the organization the header's switcher named
 * on every screen ("Acme Financial · Enterprise") although this build stores
 * no organization at all. Kept for prospect demos only; reach it through
 * `shellSample()` in ./index, which refuses when sample mode is off.
 */
export const SHELL_SAMPLE = {
  organization: { name: "Acme Financial", initials: "AF", plan: "Enterprise" },
};
